if (ANDROID)
    set(SDL_SHARED ON)
    set(SDL_STATIC OFF)
else()
    set(SDL_SHARED OFF)
    set(SDL_STATIC ON)
endif()

add_vendor(SDL)
add_vendor(spdlog)
add_vendor(stb)
add_vendor(imgui)
add_vendor(RapidXML)
add_vendor(os)
add_vendor(tree-sitter)
add_vendor(tree-sitter-cpp)
